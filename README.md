veri.csv file has the values used by the code lines. You can change any .csv file and replace it with the already existing one. odev.py is the main program.
After running the program, sonuc.txt has the outliers; arithmetical average, median, mod, IQR and etc. 
